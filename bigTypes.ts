/*
export type EngineState = {
  gameOverResult: GameOverResult | undefined;
  logEntries: LogEntry[];
  effects: GameEngineEffect[];
  numberOfPlayers: number;
  gameObjects: Map<string, BlueprintGameObjectModel>;
  bespokeTimers: Map<string, BespokeTimerState>;
  playerState: Map<string, EnginePlayerState>;
  huds: Map<string, BlueprintHudModel>;
  currentTick: number;
  tickDuration: number;
  newId: number;
  logId: number;
  randomNumberSeed: number;
  physics: Uint8Array;
};
*/
